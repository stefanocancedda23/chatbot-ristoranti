{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/appComplessa.ng",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
